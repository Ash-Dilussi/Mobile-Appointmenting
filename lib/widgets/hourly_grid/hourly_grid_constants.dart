/// Height in logical pixels representing one hour on the grid.
const double kHourHeight = 80.0;

/// Total scrollable height of the grid (24 hours).
const double kGridTotalHeight = kHourHeight * 24;

/// Width of the time label column on the left.
const double kTimeLabelWidth = 52.0;

/// Minimum card height — represents a 15-minute slot.
const double kMinCardHeight = kHourHeight * 0.25;